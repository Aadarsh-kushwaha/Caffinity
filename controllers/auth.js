// module.exports.googleCallback = (req, res) => {
//     req.flash("success", `Welcome ${req.user.name}! 🚀`);
//     res.redirect("/home");
// };

// module.exports.authFailure = (req, res) => {
//     req.flash("error", "Google login failed. Please try again.");
//     res.redirect("/user");
// };